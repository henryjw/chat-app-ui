export default async function login(username) {
    // 1) Check if username is available [2nd iteration]
    //  1a) If username not available, throw an error [2nd iteration] 
    // 2) Save username in the database [2nd iteration]
    // 3) Save username in local memory [1st iteration]
    
    throw new Error("Not yet implemented");
}